//! This module contains a utility type which is used a lot in the optional modules of this crate.
//! It's also really useful when working with serialized data.

crate::identifier!(MapIdentifier, InvalidMapID);
